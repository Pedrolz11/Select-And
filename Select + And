1- SELECT Quantity AS 'Quantidade Produto',
   ProductID AS 'Codigo do Produto'
   FROM OrderDetails
   WHERE Quantity > 15
   AND ProductID > 10;

2- SELECT OrderID AS 'Ordem Produto',
   Quantity AS 'Quantidade Produto'
   FROM OrderDetails
   WHERE OrderID < 100
   AND Quantity > 20

3- SELECT ProductID  AS 'Codigo do Produto'
   FROM OrderDetails
   WHERE ProductID >= 5
   AND ProductID <= 15
   AND ProductID %2 = 0;

4- SELECT OrderDetailID  AS 'Numeracao do Produto'
   FROM OrderDetails
   WHERE OrderDetailID > 50
   AND OrderDetailID %10 = 0

5- SELECT OrderID AS 'Ordem Produto',
   Quantity AS 'Quantidade Produto'
   FROM OrderDetails
   WHERE OrderID >= 100
   AND OrderID <= 200
   AND Quantity < 12;

6- SELECT OrderID AS 'Ordem Produto',
   Quantity AS 'Quantidade Produto'
   FROM OrderDetails
   WHERE OrderID = 300
   AND Quantity >= 10
   AND Quantity <= 20;

7- SELECT ProductID AS 'Codigo do Produto',
   Quantity AS 'Quantidade Produto'
   FROM OrderDetails
   WHERE ProductID > 20
   AND Quantity %2;

8- SELECT OrderDetailID AS 'Numeracao do Produto',
   ProductID AS 'Codigo do Produto'
   FROM OrderDetails
   WHERE ProductID < 10
   AND OrderDetailID %5 = 0;

9- SELECT Quantity AS 'Quantidade Produto',
   OrderID AS 'Ordem Produto'
   FROM OrderDetails
   WHERE Quantity > 18
   AND OrderID %10 = 3;

10- SELECT ProductID AS 'Codigo Produto',
    OrderDetailID AS 'Numeracao do Produto'
    FROM OrderDetails
    WHERE ProductID %4 = 0
    AND OrderDetailID < 200;

11- SELECT OrderID AS 'Ordem Produto',
    Quantity AS 'Quantidade Produto'
    FROM OrderDetails
    WHERE OrderID %3 = 0
    AND Quantity > 10;

12- SELECT OrderDetailID AS 'Numeracao do Produto',
    Quantity AS 'Quantidade Produto'
    FROM OrderDetails
    WHERE OrderDetailID  %10 = 6
    AND Quantity >= 20
    AND Quantity <= 40

13- SELECT OrderID AS 'Ordem Produto',
    ProductID AS 'Codigo do Produto'
    FROM OrderDetails
    WHERE OrderID > 100
    AND ProductID < 30

14- SELECT Quantity AS 'Quantidade Produto',
    OrderDetailID AS 'Numeracao do Produto'
    FROM OrderDetails
    WHERE Quantity %7 = 0
    AND OrderDetailID < 300

15- SELECT Quantity AS 'Quantidade Produto',
    OrderID AS 'Ordem Produto'
    FROM OrderDetails
    WHERE Quantity > 25
    AND OrderID >= 150
    AND OrderID <= 300;

16- SELECT Quantity AS 'Quantidade Produto',
    ProductID AS 'Codigo Produto'
    FROM OrderDetails
    WHERE Quantity < 20
    AND ProductID %10 = 5

17- SELECT OrderID AS 'Ordem Produto',
    ProductID AS 'Codigo Produto'
    FROM OrderDetails
    WHERE OrderID > 500
    AND ProductID %3 = 0;

18- SELECT OrderDetailID AS 'Numeracao do Produto',
    Quantity AS 'Quantidade Produto'
    FROM OrderDetails
    WHERE OrderDetailID >= 100
    AND OrderDetailID <= 200
    AND Quantity < 15

19- SELECT ProductID AS 'Codigo Produto',
    OrderID AS 'Ordem Produto'
    FROM OrderDetails
    WHERE ProductID > 10
    AND OrderID < 50

20- SELECT OrderDetailID AS 'Numeracao do Produto',
    Quantity AS 'Quantidade Produto'
    FROM OrderDetails
    WHERE OrderDetailID < 10
    AND Quantity %2 = 0;

21- SELECT OrderID AS 'Ordem Produto',
    ProductID AS 'Codigo Produto'
    FROM OrderDetails
    WHERE OrderID >= 300
    AND OrderID <= 400
    AND ProductID < 20;

22- SELECT Quantity AS 'Quantidade Produto',
    OrderDetailID AS 'Numeracao do Produto'
    FROM OrderDetails
    WHERE Quantity > 30
    AND OrderDetailID %10 = 8;

23- SELECT Quantity AS 'Quantidade Produto',
    OrderID AS 'Ordem Produto'
    FROM OrderDetails
    WHERE OrderID %6 = 0
    AND Quantity >= 15
    AND Quantity <= 35;

24- SELECT ProductID AS 'Codigo Produto',
    OrderDetailID AS 'Numeracao do Produto'
    FROM OrderDetails
    WHERE ProductID > 25
    AND OrderDetailID %3 = 0;

25- SELECT OrderDetailID AS 'Numeracao do Produto',
    OrderID AS 'Ordem Produto'
    FROM OrderDetails
    WHERE OrderDetailID > 400
    AND OrderID %5 = 0;

26- SELECT Quantity AS 'Quantidade Produto',
    OrderID AS 'Ordem Produto'
    FROM OrderDetails
    WHERE Quantity < 10
    AND OrderID > 50;

27- SELECT ProductID AS 'Codigo Produto',
    OrderID AS 'Ordem Produto'
    FROM OrderDetails
    WHERE ProductID >= 5
    AND ProductID <= 20
    AND OrderID %10 = 7;

28- SELECT OrderID AS 'Ordem Produto',
    Quantity AS 'Quantidade Produto'
    FROM OrderDetails
    WHERE OrderID < 200
    AND Quantity > 50;

29- SELECT ProductID AS 'Codigo Produto',
    Quantity AS 'Quantidade Produto'
    FROM OrderDetails
    WHERE ProductID %9 = 0
    AND Quantity >= 10
    AND Quantity <= 20;

30- SELECT Quantity AS 'Quantidade Produto',
    ProductID AS 'Codigo Produto'
    FROM OrderDetails
    WHERE Quantity > 50
    AND ProductID < 30;

Minha dupla que fez daqui pra baixo
31- SELECT *
    FROM OrderDetails
    WHERE OrderDetailID / 4 = 0
    AND OrderID < 300
    ORDER BY Quantity;

32- SELECT *
    FROM OrderDetails
    WHERE Quantity < 5
    AND OrderID BETWEEN 10 AND 100
    ORDER BY ProductID ASC ; 

33- SELECT *
    FROM OrderDetails
    WHERE OrderID LIKE '2%'
    AND ProductID > 15
    ORDER BY Quantity;

34- SELECT *
    FROM OrderDetails
    WHERE Quantity < 12
    AND OrderDetailID > 150
    ORDER BY OrderID;

35- SELECT *
    FROM OrderDetails
    WHERE ProductID BETWEEN 20 AND 40
    AND Quantity < 3 = 0
    ORDER BY Quantity ;

36- SELECT *
    FROM OrderDetails
    WHERE OrderID  >= 700
    AND Quantity > 40
    ORDER BY ProductID ;

37- SELECT *
    FROM OrderDetails
    WHERE ProductID / 8
    AND OrderID < 400 
    ORDER BY Quantity;

38- SELECT *
    FROM OrderDetails
    WHERE Quantity > 15
    AND OrderDetailID LIKE '4%'
    ORDER BY OrderID;

39- SELECT *
    FROM OrderDetails
    WHERE OrderID < 100 
    AND ProductID BETWEEN 10 AND 100
    ORDER BY OrderDetailID;

40- SELECT *
    FROM OrderDetails
    WHERE Quantity < 30 
    AND ProductID / 5 = 0
    ORDER BY OrderID;

41- SELECT *
    FROM OrderDetails
    WHERE ProductID LIKE '9%'
    AND Quantity < 20
    ORDER BY OrderDetailID;

42- SELECT *
    FROM OrderDetails
    WHERE OrderID / 5 = 0
    AND ProductID BETWEEN 15 AND 30
    ORDER BY Quantity;

43- SELECT *
    FROM OrderDetails
    WHERE Quantity BETWEEN 25 AND 50 
    AND OrderDetailID / 7 =  0
    ORDER BY OrderID ;

44- SELECT *
    FROM OrderDetails
    WHERE OrderID < 250
    AND Quantity / 6 = 0
    ORDER BY ProductID;

45- SELECT *
    FROM OrderDetails
    WHERE OrderDetailID BETWEEN 50 AND 150
    AND ProductID > 20
    ORDER BY Quantity;

46- SELECT *
    FROM OrderDetails
    WHERE OrderID = 350 
    AND Quantity > 30
    ORDER BY ProductID;

47- SELECT *
    FROM OrderDetails
    WHERE ProductID LIKE '0%'
    AND OrderDetailID / 2 = 0
    ORDER BY OrderID;

48- SELECT *
    FROM OrderDetails
    WHERE Quantity BETWEEN 10 AND 25
    AND ProductID < 50
    ORDER BY Quantity;

49- SELECT *
    FROM OrderDetails
    WHERE OrderID LIKE '1%'
    AND Quantity / 5 = 0
    ORDER BY OrderDetailID;

50- SELECT *
    FROM OrderDetails
    WHERE OrderDetailID > 300
    AND Quantity < 12
